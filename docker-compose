DOCKER-COMPOSE

sudo docker-compose exec nginx bash

sudo docker-compose run nginx pwd

create        build
up            down
start         stop 
restart       
run           kill
unpause       pause

ps 
images

version 
command
help 
config
top
logs

port
scale
events
pull
push

docker system df

docker volume ls                 docker volume rm           docker volume prune
docker network ls                docker network rm          docker network prune
docker ps -a                     docker rm                  docker container prune       docker rm -f $(docker ps –aq)
docker images                    docker rmi                 docker image rm $(docker image ls -q)


docker image ls -f dangling=true
docker image rm $(docker image ls -f dangling=true -q)


docker system prune
